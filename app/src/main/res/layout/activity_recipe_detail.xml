<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RecipeDetailActivity"
    android:orientation="vertical"
    android:background="@drawable/gradient"
    >

    <ImageView
        android:id="@+id/viewPagerDetailRecipe"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginTop="7dp"
        android:background="#0b752d"
        android:padding="1dp" />

    <TextView
        android:id="@+id/recipeNameDetailPage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/viewPagerDetailRecipe"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginTop="3dp"
        android:paddingRight="5dp"
        android:textSize="36sp"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/recipeIngredientsDetailPage"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:layout_below="@id/recipeNameDetailPage"
        android:layout_gravity="center"
        android:layout_marginTop="3dp" />

    <TextView
        android:id="@+id/recipeDescriptionDetailPage"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:layout_below="@id/recipeIngredientsDetailPage"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginTop="34dp"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:textSize="20sp"
        android:scrollbars="vertical"/>

</RelativeLayout>
